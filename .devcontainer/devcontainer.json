{
  "name": "OCaml",
  "image": "cuihtlauac/ocaml-devcontainer:latest",
  "customizations": {
    "vscode": {
      "extensions": [
        "ocamllabs.ocaml-platform"
      ],
      "settings": {
        "ocaml.sandbox": {
          "kind": "opam",
          "switch": "ocaml"
        }
      }
    }
  },
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}/exercises",
  "postStartCommand": "opam switch set ocaml && eval $(opam env) && code --install-extension /workspaces/${localWorkspaceFolderBasename}/.devcontainer/ocaml-platform.vsix || true"
}