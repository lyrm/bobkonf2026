(library
 (package exercise1)
 (name treiber_stack1)
 (libraries backoff utils))

; the dependency on `utils` library is here only so it is directly available
; while running the top level with `dune utop` command.
